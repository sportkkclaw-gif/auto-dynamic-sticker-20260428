# MOTION_TEMPLATE_DEFINITION_SPEC.md

Motion template 不得只是按鈕。必須有 MotionTemplateDefinition：code/name/targetLayers/parameters/keyframes/recommendedFrameCount/riskLevel。Workspace 需依 template 更新 canvas、timeline、budget。
